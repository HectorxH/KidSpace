import {ImageSourcePropType} from 'react-native';

const defaultImages: {[key: string]: {[key: string]: ImageSourcePropType}} = {
  i1: {
    uri: require('./1.png'),
  },
};

export {defaultImages};
